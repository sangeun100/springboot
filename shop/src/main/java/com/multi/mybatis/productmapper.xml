<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.multi.mapper.ProductMapper">
	
	<select id="select" parameterType="int" resultType="productVO"> 
		SELECT * FROM product WHERE ID=#{id}
	</select>
	<select id="selectall" resultType="productVO">
		SELECT * FROM product
	</select>
	<insert id="insert" parameterType="productVO">
		INSERT INTO product VALUES (#{id},#{name},#{price},SYSDATE(),#{cid})
	</insert>
	<update id="update" parameterType="productVO">
		UPDATE product SET name=#{name},pirce=#{price},cid=#{cid} WHERE ID=#{id}
	</update>
	<delete id="delete" parameterType="String">
		DELETE FROM product WHERE ID=#{id}
	</delete>
	
</mapper>






